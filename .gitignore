# Local .terraform directories
.terraform/
.terraform.lock.hcl

# Terraform state and plan (do not commit)
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Sensitive var files (use .example files only in repo)
terraform.tfvars
*.auto.tfvars
backend.azurerm.tfvars

# CLI config
.terraformrc
terraform.rc
